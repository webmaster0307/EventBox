version: '3'
services:
  nginx:
    build:
      context: .
      dockerfile: .docker/nginx.dockerfile
    depends_on:
      - server
    ports:
      - "8080:8080"
    networks:
      - eventbox-net
  server:
    build:
      context: ./
      dockerfile: ./Dockerfile
    image:
      eventbox-server:v2
    env_file: 
      - .env
    expose:
      - ${SERVER_PORT}
    networks:
    - eventbox-net
    restart: always
    command: npm run start-prod
networks:
  eventbox-net:
    driver: bridge